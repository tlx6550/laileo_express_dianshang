extends ../layout

block content
    .container
        .row
            div.panel.panel-info  
                div.panel-heading
                    h3.panel-title 商品展示页
                    div.btn-group.pull-right#mygroup
                        a.btn.btn-default(role="button",href="/admin/commodity/new") 添加商品
                        a.btn.btn-default(role="button",href="") 购物车
                        a.btn.btn-default.btn-warning(role="button",href="/logout") 退出
                div.panel-body
                    each item in commodities
                        div.col-md-4
                            a.thumbnail(href="/commodity/#{item._id}")
                                img(src="/upload/#{item.imgScr}",alt="#{item.name}")
                            .caption
                                h4 #{item.name}
                                strong 价格¥
                                span.com-pric #{item.price}
                                p: a.btn.btn-primary(href="/commodity/#{item._id}",role="button") 加入购物车
    script(src="/javascripts/admin.js")
 